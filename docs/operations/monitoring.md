# CodeGen AI 모니터링 & 운영 가이드

> **Version**: 1.0.0
> **Last Updated**: 2026-01-31
> **Author**: 🔧 DevOps Expert Persona
> **Status**: Production Ready

---

## 목차

1. [모니터링 개요](#모니터링-개요)
2. [메트릭 시스템](#메트릭-시스템)
3. [로깅 전략](#로깅-전략)
4. [알림 설정](#알림-설정)
5. [대시보드](#대시보드)
6. [장애 대응](#장애-대응)
7. [성능 최적화](#성능-최적화)
8. [운영 체크리스트](#운영-체크리스트)

---

## 모니터링 개요

### 관측성 3요소 (Three Pillars of Observability)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Observability Pillars                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│    📊 Metrics              📝 Logs                 🔍 Traces            │
│    ───────────             ────────                ─────────            │
│    "무엇이 발생했나?"       "왜 발생했나?"          "어디서 발생했나?"    │
│                                                                         │
│    • 요청 수               • 에러 상세             • 요청 경로          │
│    • 응답 시간             • 디버그 정보           • 지연 원인          │
│    • 에러율                • 감사 로그             • 의존성 관계        │
│    • 리소스 사용량         • 비즈니스 이벤트       • 병목 지점          │
│                                                                         │
│    도구: Vercel Analytics  도구: 구조화 로거       도구: Sentry        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 모니터링 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Monitoring Architecture                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐                │
│  │   Vercel     │   │   Sentry     │   │    Slack     │                │
│  │   Analytics  │   │              │   │   Webhook    │                │
│  └──────┬───────┘   └──────┬───────┘   └──────┬───────┘                │
│         │                   │                   │                       │
│         │    Metrics        │    Errors         │    Alerts             │
│         │                   │                   │                       │
│         ▼                   ▼                   ▼                       │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                     CodeGen AI Application                       │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐             │   │
│  │  │ Pages   │  │ Actions │  │ API     │  │ Cron    │             │   │
│  │  │         │  │         │  │ Routes  │  │ Jobs    │             │   │
│  │  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘             │   │
│  │       │            │            │            │                   │   │
│  │       └────────────┴────────────┴────────────┘                   │   │
│  │                           │                                      │   │
│  │                    ┌──────┴──────┐                               │   │
│  │                    │   Logger    │                               │   │
│  │                    │  (Pino)     │                               │   │
│  │                    └─────────────┘                               │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│         │                   │                   │                       │
│         ▼                   ▼                   ▼                       │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐                │
│  │   Supabase   │   │   External   │   │ TossPayments │                │
│  │   (DB/Auth)  │   │   AI APIs    │   │   Webhook    │                │
│  └──────────────┘   └──────────────┘   └──────────────┘                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 메트릭 시스템

### 핵심 비즈니스 메트릭

| 메트릭 | 설명 | 목표 | 알림 조건 |
|--------|------|------|----------|
| `content_generation_total` | 총 콘텐츠 생성 수 | - | - |
| `content_generation_success_rate` | 생성 성공률 | > 95% | < 90% |
| `payment_success_rate` | 결제 성공률 | > 98% | < 95% |
| `daily_active_users` | 일일 활성 사용자 | 성장 | -20% |
| `credits_consumed_daily` | 일일 크레딧 소비 | - | -50% |
| `revenue_daily` | 일일 매출 | 성장 | -30% |

### 기술 메트릭

| 메트릭 | 설명 | 목표 | 알림 조건 |
|--------|------|------|----------|
| `api_latency_p50` | 50% 응답 시간 | < 200ms | > 500ms |
| `api_latency_p95` | 95% 응답 시간 | < 1s | > 3s |
| `api_latency_p99` | 99% 응답 시간 | < 3s | > 10s |
| `error_rate` | 에러율 | < 1% | > 5% |
| `ai_token_usage_daily` | AI 토큰 사용량 | < 예산 | > 80% |
| `rate_limit_exceeded` | Rate Limit 초과 | < 1% | > 5% |

### Vercel Analytics 활용

```typescript
// 커스텀 메트릭 전송
import { track } from '@vercel/analytics';

// 콘텐츠 생성 완료 추적
track('content_generated', {
  language: 'PYTHON',
  difficulty: 'INTERMEDIATE',
  generationTimeMs: 5432,
  tokensUsed: 1234,
});

// 결제 완료 추적
track('payment_completed', {
  type: 'credit_purchase',
  amount: 24900,
  packageId: 'standard',
});
```

### Core Web Vitals

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Core Web Vitals Targets                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  LCP (Largest Contentful Paint)                                         │
│  ├─ 목표: < 2.5초                                                        │
│  ├─ 현재: 측정 필요                                                      │
│  └─ 개선: 이미지 최적화, 서버 응답 속도                                   │
│                                                                         │
│  FID (First Input Delay)                                                │
│  ├─ 목표: < 100ms                                                       │
│  ├─ 현재: 측정 필요                                                      │
│  └─ 개선: JavaScript 최적화, 코드 분할                                   │
│                                                                         │
│  CLS (Cumulative Layout Shift)                                          │
│  ├─ 목표: < 0.1                                                         │
│  ├─ 현재: 측정 필요                                                      │
│  └─ 개선: 이미지 크기 명시, 폰트 로딩 최적화                              │
│                                                                         │
│  INP (Interaction to Next Paint)                                        │
│  ├─ 목표: < 200ms                                                       │
│  ├─ 현재: 측정 필요                                                      │
│  └─ 개선: 이벤트 핸들러 최적화, 상태 업데이트 최적화                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 로깅 전략

### 로그 레벨

| 레벨 | 용도 | 예시 |
|------|------|------|
| `debug` | 개발/디버깅 | 함수 진입/종료, 변수 값 |
| `info` | 일반 정보 | 요청 완료, 작업 성공 |
| `warn` | 경고 | Rate Limit 근접, 재시도 |
| `error` | 오류 | 예외 발생, 실패 |

### 구조화된 로깅

```typescript
// src/lib/logger.ts

import pino from 'pino';

const logger = pino({
  level: process.env.LOG_LEVEL || 'info',
  formatters: {
    level: (label) => ({ level: label }),
  },
  timestamp: pino.stdTimeFunctions.isoTime,
});

// 사용 예시
export function logInfo(message: string, context?: Record<string, unknown>) {
  logger.info(maskSensitiveData(context), message);
}

export function logError(message: string, error: Error, context?: Record<string, unknown>) {
  logger.error({
    ...maskSensitiveData(context),
    error: {
      name: error.name,
      message: error.message,
      stack: process.env.NODE_ENV === 'development' ? error.stack : undefined,
    },
  }, message);
}
```

### 로그 포맷

```json
{
  "level": "info",
  "time": "2026-01-31T10:30:00.000Z",
  "msg": "콘텐츠 생성 완료",
  "context": {
    "userId": "user_123",
    "contentId": "content_456",
    "language": "PYTHON",
    "generationTimeMs": 5432,
    "tokensUsed": 1234
  }
}
```

### 로그 필터링

```bash
# Vercel 로그 조회
vercel logs --filter "level=error"
vercel logs --filter "context.userId=user_123"

# 로컬 개발
npm run dev 2>&1 | jq 'select(.level == "error")'
```

---

## 알림 설정

### 알림 우선순위

| 우선순위 | 응답 시간 | 채널 | 예시 |
|----------|----------|------|------|
| P1 | 즉시 | Slack + 전화 | 서비스 다운, 데이터 유출 |
| P2 | 1시간 | Slack | 에러율 급증, 결제 실패 급증 |
| P3 | 4시간 | Slack | 성능 저하, Rate Limit 급증 |
| P4 | 24시간 | 이메일 | 일일 리포트, 트렌드 경고 |

### Slack Webhook 설정

```typescript
// src/lib/notification/slack.ts

const SLACK_WEBHOOK_URL = process.env.SLACK_ADMIN_WEBHOOK_URL;

interface SlackAlert {
  title: string;
  message: string;
  severity: 'critical' | 'warning' | 'info';
  fields?: Record<string, string>;
}

export async function sendSlackAlert(alert: SlackAlert) {
  if (!SLACK_WEBHOOK_URL) return;

  const color = {
    critical: '#FF0000',
    warning: '#FFA500',
    info: '#0000FF',
  }[alert.severity];

  await fetch(SLACK_WEBHOOK_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      attachments: [{
        color,
        title: alert.title,
        text: alert.message,
        fields: alert.fields
          ? Object.entries(alert.fields).map(([title, value]) => ({
              title,
              value,
              short: true,
            }))
          : undefined,
        ts: Math.floor(Date.now() / 1000),
      }],
    }),
  });
}

// 사용 예시
await sendSlackAlert({
  title: '🚨 결제 실패율 증가',
  message: '최근 1시간 결제 실패율이 10%를 초과했습니다.',
  severity: 'critical',
  fields: {
    '실패율': '12.5%',
    '총 시도': '150건',
    '실패': '19건',
  },
});
```

### Sentry 알림 설정

```typescript
// src/instrumentation.ts

import * as Sentry from '@sentry/nextjs';

export function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    Sentry.init({
      dsn: process.env.SENTRY_DSN,
      environment: process.env.NODE_ENV,
      tracesSampleRate: 0.1,
      profilesSampleRate: 0.1,

      // 알림 필터링
      beforeSend(event) {
        // 무시할 에러 필터
        if (event.message?.includes('Rate limit exceeded')) {
          return null;
        }
        return event;
      },

      // 알림 그룹핑
      fingerprint: ['{{ default }}', '{{ type }}'],
    });
  }
}
```

---

## 대시보드

### 주요 대시보드

#### 1. 비즈니스 대시보드

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Business Dashboard                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │   DAU       │  │   매출      │  │   생성 수   │  │  전환율     │   │
│  │   1,234     │  │ ₩2.5M      │  │   5,678     │  │   3.2%      │   │
│  │   ↑ 12%     │  │   ↑ 8%     │  │   ↑ 15%    │  │   ↓ 0.3%   │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
│                                                                         │
│  [일일 생성 추이 그래프]                                                 │
│  ─────────────────────────────────────────────────────────────────     │
│                                                                         │
│  [플랜별 사용자 분포]              [언어별 생성 분포]                    │
│  ┌────────────────────┐            ┌────────────────────┐              │
│  │ Starter: 70%       │            │ Python: 45%        │              │
│  │ Pro: 25%           │            │ JavaScript: 30%    │              │
│  │ Team: 5%           │            │ SQL: 15%           │              │
│  └────────────────────┘            │ Others: 10%        │              │
│                                    └────────────────────┘              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 2. 기술 대시보드

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Technical Dashboard                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  에러율     │  │  p95 응답   │  │  토큰 사용  │  │  Rate Limit │   │
│  │   0.5%     │  │   1.2s      │  │   850K      │  │   23회      │   │
│  │   ↓ 0.2%   │  │   ↓ 0.3s   │  │   ↑ 12%    │  │   ↓ 50%    │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
│                                                                         │
│  [응답 시간 분포]                   [에러 유형 분포]                     │
│  ─────────────────────────────────  ─────────────────────────────────   │
│  p50: ████████ 200ms               AUTH_ERROR: ████ 40%                 │
│  p95: ██████████████ 1.2s          VALIDATION: ███ 30%                  │
│  p99: ████████████████████ 3s      AI_ERROR: ██ 20%                     │
│                                    OTHER: █ 10%                         │
│                                                                         │
│  [최근 에러 로그]                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 10:30:15 ERROR AI 생성 실패 - user_123 - 타임아웃                 │  │
│  │ 10:28:42 WARN  Rate Limit 초과 - 192.168.1.1                      │  │
│  │ 10:25:33 ERROR 결제 검증 실패 - order_456 - 금액 불일치           │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Vercel Dashboard 활용

| 탭 | 주요 확인 항목 |
|-----|--------------|
| Analytics | 페이지 뷰, 방문자, 이탈률 |
| Speed Insights | Core Web Vitals, 로딩 시간 |
| Logs | 실시간 로그, 에러 필터링 |
| Functions | Serverless 함수 실행 시간, 콜드 스타트 |

---

## 장애 대응

### 장애 등급

| 등급 | 정의 | 목표 복구 시간 |
|------|------|--------------|
| P1 | 서비스 완전 중단 | 30분 |
| P2 | 핵심 기능 장애 (결제, AI 생성) | 2시간 |
| P3 | 부가 기능 장애 | 4시간 |
| P4 | 성능 저하 | 24시간 |

### 장애 대응 플로우

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Incident Response Flow                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 탐지 (Detection)                                                    │
│     ├─ 자동 알림 (Sentry, Vercel)                                       │
│     ├─ 사용자 신고                                                       │
│     └─ 모니터링 대시보드                                                 │
│              │                                                          │
│              ▼                                                          │
│  2. 확인 (Acknowledgment)                                               │
│     ├─ 장애 등급 분류                                                    │
│     ├─ 담당자 지정                                                       │
│     └─ 상태 페이지 업데이트                                              │
│              │                                                          │
│              ▼                                                          │
│  3. 진단 (Diagnosis)                                                    │
│     ├─ 로그 분석                                                         │
│     ├─ 메트릭 확인                                                       │
│     └─ 최근 배포 확인                                                    │
│              │                                                          │
│              ▼                                                          │
│  4. 복구 (Recovery)                                                     │
│     ├─ 롤백 (필요시)                                                     │
│     ├─ 핫픽스 적용                                                       │
│     └─ 서비스 정상화 확인                                                │
│              │                                                          │
│              ▼                                                          │
│  5. 사후 분석 (Post-mortem)                                             │
│     ├─ 타임라인 정리                                                     │
│     ├─ 근본 원인 분석                                                    │
│     └─ 재발 방지 대책                                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 롤백 절차

```bash
# 1. 현재 배포 확인
vercel ls

# 2. 이전 배포 목록 확인
vercel ls --all

# 3. 롤백 실행
vercel rollback [deployment-url]

# 4. 롤백 확인
vercel inspect [deployment-url]
```

### 상태 페이지 업데이트

```typescript
// 상태 업데이트 예시 (Statuspage.io 또는 자체 구현)
await updateStatusPage({
  status: 'investigating',
  title: 'AI 콘텐츠 생성 지연',
  message: '현재 AI 콘텐츠 생성 기능에 지연이 발생하고 있습니다. 원인을 조사 중입니다.',
  affectedComponents: ['ai-generation'],
});
```

---

## 성능 최적화

### 병목 지점 식별

| 지점 | 모니터링 방법 | 최적화 전략 |
|------|-------------|-----------|
| DB 쿼리 | Supabase Dashboard | 인덱스 추가, 쿼리 최적화 |
| AI API | 응답 시간 로깅 | 타임아웃 조정, 폴백 |
| 이미지 | Core Web Vitals | Next.js Image 최적화 |
| 번들 크기 | Bundle Analyzer | 코드 스플리팅, 트리 셰이킹 |

### 캐싱 전략

```typescript
// 정적 페이지 캐싱
export const revalidate = 3600; // 1시간

// API 응답 캐싱
return new Response(data, {
  headers: {
    'Cache-Control': 'public, s-maxage=60, stale-while-revalidate=300',
  },
});

// 데이터 캐싱 (향후 TanStack Query)
const { data } = useQuery({
  queryKey: ['user', userId],
  queryFn: () => getUser(userId),
  staleTime: 5 * 60 * 1000, // 5분
});
```

---

## 운영 체크리스트

### 일일 체크리스트

- [ ] 에러율 확인 (< 1%)
- [ ] 응답 시간 확인 (p95 < 1s)
- [ ] 결제 성공률 확인 (> 98%)
- [ ] AI 토큰 사용량 확인
- [ ] Rate Limit 초과 확인
- [ ] 보안 알림 검토

### 주간 체크리스트

- [ ] 트렌드 분석 (DAU, 매출)
- [ ] 에러 패턴 분석
- [ ] 성능 리포트 검토
- [ ] 의존성 보안 업데이트 확인
- [ ] 백업 상태 확인
- [ ] 비용 분석

### 배포 전 체크리스트

- [ ] 모든 테스트 통과
- [ ] 스테이징 환경 검증
- [ ] 롤백 계획 준비
- [ ] 모니터링 알림 활성화
- [ ] 담당자 대기

---

## Cron Jobs 관리

### 현재 Cron Jobs

| Job | 스케줄 | 설명 | 모니터링 |
|-----|--------|------|----------|
| `reset-daily-limits` | 매일 00:00 UTC | 일일 생성 한도 리셋 | 실행 완료 로그 |
| `renew-subscriptions` | 6시간마다 | 구독 자동 갱신 | 갱신 성공/실패 수 |
| `expire-credits` | 매일 01:00 UTC | 만료 크레딧 정리 | 만료 크레딧 수 |
| `cleanup-webhooks` | 매일 02:00 UTC | 오래된 웹훅 로그 삭제 | 삭제 건수 |

### Cron Job 모니터링

```typescript
// Cron Job 래퍼
export async function runCronJob(
  name: string,
  job: () => Promise<void>
) {
  const startTime = Date.now();

  try {
    await job();

    logInfo(`Cron Job 완료: ${name}`, {
      duration: Date.now() - startTime,
    });
  } catch (error) {
    logError(`Cron Job 실패: ${name}`, error as Error);

    await sendSlackAlert({
      title: `🚨 Cron Job 실패: ${name}`,
      message: (error as Error).message,
      severity: 'critical',
    });
  }
}
```

---

## 참고 자료

- [Vercel Monitoring](https://vercel.com/docs/analytics)
- [Sentry Documentation](https://docs.sentry.io/)
- [Supabase Dashboard](https://supabase.com/docs/guides/platform/logs)
- [Site Reliability Engineering](https://sre.google/sre-book/table-of-contents/)

---

*마지막 업데이트: 2026-01-31*
